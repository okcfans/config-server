#指定端口号
server:
  port: 8771
#指定eureka服务地址
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

#指定服务的名称
spring:
  application:
    name: product-service
env: dev
branch: test
